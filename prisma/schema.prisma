generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model User {
  name             String
  createdAt        DateTime          @default(now())
  password         String
  role             Role              @default(BORROWER)
  status           LoanStatus?
  updatedAt        DateTime          @updatedAt
  user_id          String            @id @db.VarChar(32)
  username         String            @unique
  email            String?
  noHandphone      String?
  loans            Loan[]            @relation("UserLoans")
  loanParticipants LoanParticipant[]
}

model Category {
  category_id   String    @id @db.VarChar(32)
  category_name String    @unique
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  products      Product[]
}

model Product {
  product_id      String     @id @db.VarChar(32)
  product_name    String     @unique
  product_image   String?
  quantity        Int
  category_id     String
  createdAt       DateTime   @default(now())
  updatedAt       DateTime   @updatedAt
  product_avaible Int
  loanItems       LoanItem[]
  category        Category   @relation(fields: [category_id], references: [category_id], onDelete: Cascade)
}

model Loan {
  loan_id      String            @id @db.VarChar(32)
  borrower_id  String
  created_at   DateTime          @default(now())
  updated_at   DateTime          @updatedAt
  status       LoanStatus        @default(REQUESTED)
  borrower     User              @relation("UserLoans", fields: [borrower_id], references: [user_id], onDelete: Cascade)
  items        LoanItem[]
  participants LoanParticipant[]
  report       Report?
}

model LoanItem {
  loan_item_id String  @id @db.VarChar(32)
  product_id   String
  quantity     Int
  loan_id      String
  loan         Loan    @relation(fields: [loan_id], references: [loan_id], onDelete: Cascade)
  product      Product @relation(fields: [product_id], references: [product_id], onDelete: Cascade)
}

model LoanParticipant {
  id         String          @id @db.VarChar(32)
  loan_id    String
  user_id    String
  created_at DateTime        @default(now())
  role       ParticipantRole
  loan       Loan            @relation(fields: [loan_id], references: [loan_id], onDelete: Cascade)
  user       User            @relation(fields: [user_id], references: [user_id], onDelete: Cascade)

  @@unique([loan_id, user_id])
}

model Report {
  report_id          String   @id @db.VarChar(32)
  loan_id            String   @unique
  spt_file           String?
  spt_number         String
  destination        String
  place_of_execution String
  start_date         DateTime
  end_date           DateTime
  created_at         DateTime @default(now())
  updated_at         DateTime @updatedAt
  loan               Loan     @relation(fields: [loan_id], references: [loan_id], onDelete: Cascade)
}

enum Role {
  SUPERADMIN
  ADMIN
  BORROWER
}

enum LoanStatus {
  REQUESTED
  APPROVED
  REJECTED
  RETURNED
}

enum ParticipantRole {
  OWNER
  INVITED
}
